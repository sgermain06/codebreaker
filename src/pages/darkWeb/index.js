import React from "react";
import { withRouter } from "react-router";

import PageHeader from '../../components/pageHeader';

function DarkWeb() {
    return <PageHeader />;
}

export default withRouter(DarkWeb);
