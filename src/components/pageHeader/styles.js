export default function styles(theme) {
    return {
        container: {
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center'
        },
        title: {
            fontSize: 45,
            padding: theme.spacing(1),
            margin: 0,
        }
    };
};