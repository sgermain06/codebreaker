export default function styles(theme) {
    return {
        footer: {
            flexGrow: 0,
            flexShrink: 1,
            flexBasis: "40px",
            paddingTop: theme.spacing(1),
        },
    };
}
